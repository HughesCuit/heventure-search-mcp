# Improve Cycle Report — 2026-05-04

## Summary

| Metric | Value |
|--------|-------|
| Issues Attempted | 3 (#22, #21, #3) |
| Issues Succeeded | 3 |
| Issues Skipped | 4 (#23, #11, #10, #4) |
| CI Runs | 1 (pass) |
| CI Retries | 0 |
| New Tests Added | 17 |
| Total Tests | 44 (all passing) |

## Issues Resolved

### #22 — lxml is a required dependency but unused in server.py
- **Commit**: `c8e153e`
- **Change**: Removed `lxml>=4.9.0` from `dependencies` in `pyproject.toml`, added as optional dependency `lxml-parser`
- **Rationale**: All 4 BeautifulSoup calls in `server.py` use `html.parser`, never `lxml`. lxml is a C-extension heavy library that causes installation issues on Windows/Alpine.
- **Review**: PASS — clean removal, no code changes needed, all tests pass

### #21 — Missing unit tests for handle_call_tool MCP dispatch
- **Commit**: `f68d1c7`
- **Change**: Added 17 new tests across 2 test classes:
  - `TestHandleListTools` (3 tests): tool count, web_search schema, get_webpage_content schema
  - `TestHandleCallTool` (14 tests): empty query, missing query, None args, duckduckgo/bing/google/both engines, SerpAPI key auto-append, no results, get_webpage_content success/empty URL/empty content, unknown tool, URL dedup
- **Review**: PASS — proper mocking of WebSearcher async context manager, no network calls

### #3 — tavily 和 aiohttp-socks 应为可选依赖
- **Commit**: `c8e153e` (fixed alongside #22)
- **Change**: Moved `aiohttp-socks>=0.8.4` and `tavily>=1.0.0` from `dependencies` to `[project.optional-dependencies]` as `socks` and `tavily` extras
- **Rationale**: These are API-specific dependencies not needed by all users

## Issues Skipped (4 remaining open)

| # | Title | Reason |
|---|-------|--------|
| 23 | DuckDuckGo fallback path lacks within-engine URL dedup | Out of scope for this cycle |
| 11 | pyproject.toml classifiers 声明 Python 3.8/3.9 但 requires-python >= 3.10 | Out of scope for this cycle |
| 10 | CHANGELOG.md 存在重复章节和版本号 | Out of scope for this cycle |
| 4 | 缓存键未考虑查询参数顺序 | Out of scope for this cycle |

## CI Status

- **Run ID**: 25284065794
- **Conclusion**: ✅ success
- **Retries**: 0 (passed on first attempt)

## Files Modified

- `pyproject.toml` — dependency restructuring
- `tests/test_server.py` — +356 lines, 17 new tests
- `CHANGELOG.md` — documented all 3 fixes
